#!/usr/bin/env bash

# remove the files generated by the build process
rm -Rf m4/ autom4te.cache/ .libs
rm crumbs-1.0.tar.gz config.log libtool config.status Makefile.in depcomp COPYING INSTALL missing install-sh config.sub config.guess compile ar-lib configure aclocal.m4 ltmain.sh Makefile libcrumbs.la "configure~"
rm impl/*.o impl/*.lo impl/.dirstamp
rm -Rf impl/.deps

# remove the files required by autoreconf
rm AUTHORS README NEWS ChangeLog configure.ac Makefile.am

